# PDF и чекпойнты - бинарные. Без этого git считает их текстом (в первых
# 8000 байт нет NUL), показывает многотысячные diff и при core.autocrlf=input
# срезает CR внутри бинарных потоков, портя файл.
*.pdf binary
*.pt binary
